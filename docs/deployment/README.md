# Deployment documents

These files describe current or historical V1 environments and deployment procedures. Preserve verified secret boundaries, Frontend → FastAPI → external-service separation, CORS, health checks, ownership checks, and cleanup discipline.

Re-verify platform versions, environment variables, URLs, project identifiers, and commands before use. Nothing in this directory defines V2 product behavior or the future V2 architecture.
