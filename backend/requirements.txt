# Codize backend — Milestone 4 foundation.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic-settings>=2.5
# JWT verification against Supabase JWKS (ES256) — [crypto] pulls in `cryptography`.
PyJWT[crypto]>=2.9

# Test dependencies (kept together — the backend has no separate dev image yet).
pytest>=8.3
httpx>=0.27
