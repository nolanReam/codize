# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this repo is

**Latest completed milestone: M16B.3B (2026-07-14).** The existing Evidence
page now exposes the reviewed M16B.3A Verification-to-Evidence handoff while
preserving legacy/manual Evidence. This is a frontend-only milestone; M16S.1
write-boundary hardening remains in force. See
`.claude/memory/linked-evidence-ui-conventions.md` and
`.claude/memory/workflow-artifact-write-boundary.md`.

M16S.1 is write-boundary hardening only: the forward migration makes
`public.projects` authenticated-owner read-only at the Postgres grant layer
while preserving owner RLS reads and trusted FastAPI writes. The shared hosted
pilot database was inspected but not migrated because it is not an identified
safe development/test target; deployment plus effective privilege verification
remains required.

Codize is an educational platform that helps students understand the projects they build with AI — it teaches real dev workflows and reasoning about your own code, using AI as a tool rather than a crutch. It is a benign educational product about architecture understanding and project reasoning. It is **not** a cybersecurity, exploit, malware, or offensive-security tool.

**Product direction:** Codize is a prevention-first, recovery-capable AI coding workflow trainer for beginner and early-intermediate student builders. The stable product direction lives in `docs/context/codize_product_operating_brief_v2.md`; use `docs/context/context_authority.md` to resolve source conflicts and inspect repository reality for implementation state.

**Current state: Milestone 16B.2 complete** (workflow artifact store, on top of M12's evaluation, M11's reconnection, M10's functional unlocks, M9's Interrogation Gate, M8's phase workspace, M7's roadmap engine, M6's intake engine, M5's template engine, M4's FastAPI core, M3's auth foundation, M2's schema+RLS and M1's pre-build artifacts). The FastAPI backend lives in `backend/app/` (see `backend/README.md`): app factory + CORS + consistent error shape (`main.py`, `core/errors.py`), centralized `Settings` with SecretStr server-only values (`core/config.py`), Supabase JWT verification via JWKS/ES256 (`core/security.py`), and the `require_user` auth dependency → 401 (`deps/auth.py`). The template engine (`services/template_service.py`) loads and validates the three archetype templates at startup (fail-fast; rejects any fourth archetype), serves them as copies (no runtime mutation), and holds the spec's deterministic classification tiebreaker (`resolve_archetype`). The intake engine (`services/intake_service.py`) holds the five spec-verbatim questions, enforces sequential first-answering (Q1 unskippable; since M13E.1 an already-answered question may be revised until completion — classification always runs on the final stored answers), normalizes answers, stores them on the `projects` row via the `ProjectRepository` seam (`services/project_repository.py` — PostgREST with the service-role/secret key, every query ownership-filtered by `user_id`; **live writes verified end-to-end in M9** with the newer `sb_secret_...` key format, which the backend treats as opaque; the module also holds the `GateSessionRepository` since M9), and on completion classifies the project via a deterministic keyword fallback into `resolve_archetype` (the temp-0 LLM classification call replaces `_derive_classification_signals` in a later milestone). All LLM traffic goes through the provider-agnostic `services/llm_service.py`: Gemini primary (`GEMINI_API_KEY`/`GEMINI_MODEL`), OpenRouter fallback (`OPENROUTER_API_KEY`/`OPENROUTER_MODEL`), deterministic stub only for tests/no-key mode — **Anthropic is intentionally not supported**. The roadmap engine (`services/roadmap_service.py`) requires completed intake + archetype, fills the `roadmap_generation.md` prompt with the archetype template and verbatim intake answers, calls the LLM at temperature 0.7, validates the output against the source template and **fails closed on any structural drift** (phase count/order/numbers/titles, gate targets, gate depth, unlock conditions, AI/human task counts, verbatim NOTE:/RLS-first security constraints, archetype id), then persists the roadmap JSONB and flips `projects.status` 'intake' → 'active' in the same write; **live Gemini generation verified in M8** (4 real calls: 3 clean roadmaps, 1 drifted output correctly discarded by the fail-closed validator). **M13C.1B (2026-07-04) — roadmap reliability hotfix:** drift/failure no longer blocks the user. `generate_roadmap` makes one LLM personalization attempt; on provider error, unparseable output, or structural drift it falls back to `build_fallback_roadmap` — a deterministic, structurally-valid roadmap built straight from the archetype template (intake purpose substituted into the template's `[PROJECT_PURPOSE]`/`[PROJECT_SCALE]` slots, which live only in personalizable fields, so it always passes the **unchanged** validator; re-validated before storage). Invalid LLM output is still never stored; the fallback is silent-but-logged; temperature stays 0.7. Live-verified with real Gemini (4 runs: 2 personalized, 2 fallback, all → active/valid/7-phase, zero blocks). A stronger `GEMINI_MODEL` can be set via env to raise the personalization rate (provider-agnostic; no paid model hardcoded). OpenRouter fallback still live-unverified since Gemini never failed. The phase workspace (`services/phase_service.py`) serves phases straight from the stored roadmap (no LLM call — `phase_explanation.md` prose is deliberately not wired up yet); eligibility is intake complete + archetype + roadmap + status 'active'; task completion lives in the separate `projects.task_progress` JSONB column (`{"<phase>": ["ai-1", "human-2", …]}`, 1-based ids into the phase's task lists) so ticking tasks can never mutate the fixed roadmap structure, unknown ids are dropped on read, and `current_phase` is advanced only by the Interrogation Gate, never by task completion. The Interrogation Gate (`services/gate_service.py`, M9) runs the spec's 3-turn flow for the current phase: eligibility + 30-minute cooldown check (derived from `gate_sessions.failed_at`, no cooldown table), then anchor collection (deterministic server-side concrete-element check plus model re-validation via an `ANCHOR_REJECTED:` marker — no Turn 1 question is ever generated without a valid anchor), then turns 1–3 at temperature 0.3 (`gate_turn_1/2/3.md`; each turn stores the previous answer and the next question in one write so LLM failures are retryable with nothing lost), then a **separate temperature-0 evaluator call** (`gate_evaluation.md`) parsed strict/fail-closed (malformed verdict → 502, nothing stored). On PASS: `passed_at`, `gate_history_summary` appended (attempt counts only — no scores, the column is client-readable), `current_phase` +1 but never past the final phase. On FAIL: `failed_at` (starts the cooldown), no advancement. The 0–10 quality score is stored on `gate_sessions.score` for the hidden unlock thresholds and **never appears in any API response** (the DB also revokes the column from client roles). **Live-verified in M9 with real Gemini**: one full PASS gate (implementation-specific hypothetical, phase advanced 1→2) and one full FAIL gate (textbook answer auto-failed Condition 3, cooldown enforced live), plus the spec's 10 live adversarial prompt cases — all defended after two prompt strengthenings recorded in `docs/prebuild/adversarial_tests.md` (re-runnable via `scripts/live_adversarial_tests.py`). The functional unlock engine (`services/unlock_service.py`, M10) implements the spec's variable-reward mechanic with **no schema change** (the M2 `unlocks` table already fit): after every gate PASS (never a FAIL) the gate flow evaluates unlocks by recomputing from the project's passed-gate history — the hidden rule (spec's resolved decision) is a quality score ≥7 on **two consecutive phases' passed gates** (only the passing attempt's score counts; a single high score never qualifies; phase 1 alone never qualifies), and qualifying at phase N grants roadmap phase N's `functional_unlock` reward (template content: skip-configuration / pre-built component — the spec's two MVP example types). Grants are idempotent and self-healing: recompute-then-insert-missing plus the DB's unique `(project_id, unlock_key)` with PostgREST `resolution=ignore-duplicates`, and an unlock storage error never fails the PASS response (logged; the next PASS backfills). Unlocks never mutate the roadmap and never advance phases; the threshold, rule, and raw scores are server-only and appear in no response — client views carry only id/unlock_key/phase/description/unlocked_at/project_id (the `unlocks` table is owner read-only, client writes revoked since M2). **Live-verified in M10** (10/10 smoke checks): real grant after scores 8,7 on consecutive phases, idempotent re-evaluation, duplicate insert ignored by the constraint, RLS client reads (owner sees own row with no score field, other user sees zero). The reconnection engine (`services/reconnection_service.py`, M11) implements the spec's 72-hour Yeager mechanic server-side with **no schema change**: `profiles.last_login_at` means **"last acknowledged presence"** — initialized by the signup trigger's `default now()` (a brand-new user never sees the modal) and written ONLY by `POST /reconnection/acknowledge`; `GET /reconnection` is a pure read, so checking state can never suppress the modal (frontend contract for M13: GET first on every login, then acknowledge — immediately when not needed, on the "Let's keep building" click when needed). The GET returns one of four controlled 200 states: `new_user`, `recently_active` (< 72h), `workspace_not_ready` (away 72h+ but no active project/roadmap yet), or `reconnection` (away 72h+) with a deterministic LLM-free safe summary — the verbatim intake purpose (spec requirement), current phase number/title/core-concept reminder, incomplete current-phase tasks, the newest gate-history line (attempt counts only), earned unlock views, and a recommended next action; never scores, thresholds, prompts, or keys. Reconnection never mutates the roadmap, never advances phases, never grants unlocks. **Live-verified in M11** (12/12 smoke checks: trigger-fresh timestamp → recently_active, backdated 100h → workspace_not_ready then full reconnection summary, idempotent acknowledge, per-user isolation, RLS profile reads with real JWTs). The evaluation engine (`services/evaluation_service.py`, M12) is a **deterministic, computed-on-read progress evaluation** — no LLM call, no persistence, no schema change (the spec defines no evaluation snapshot to store, and its tracking section forbids showing process metrics as numbers/scores): `GET /evaluation` returns one controlled 200 state — `not_started` / `intake_needed` / `roadmap_needed` pre-active, then `in_progress`, `gate_ready` (all current-phase tasks done, or a gate session mid-flight → resume), `cooldown` (reuses `gate_service.cooldown_remaining`, public since M12, and adds `cooldown_seconds_remaining`), or `complete` (final phase's gate passed) — never an error. Content comes only from already-client-visible state via the shared safe-view seams (`phase_service.current_phase_view` + the new public `phase_service.incomplete_tasks`, `unlock_service.unlock_views`): phase number/title, completed/total phase and task counts, incomplete tasks, a `recent_gate` label (newest `gate_history_summary` line for passes — attempt counts only — or the evaluator's already-client-visible one-sentence reason for fails; never a score), earned unlock views, and a recommended `next_action`. Evaluation is a pure read: it never mutates roadmap/tasks/gates/unlocks, never advances phases, and never touches `last_login_at` (it takes no ProfileRepository at all). **Live-verified in M12** (12/12 smoke checks: full lifecycle state walk, live task tick reflected, consecutive-quality unlock surfaced safely, cooldown after a FAIL, leak-free JSON, pure-read row comparison, per-user isolation). The workflow artifact store (`services/workflow_service.py`, M13B) persists the student-authored v3 Build Loop sections (four in M13B; `implementation_import` added in M15A) — `prompt_builder`, `review_board`, `evidence`, `verification` — phase-scoped in the `projects.workflow_artifacts` JSONB column (migration `20260703130000`, exact `task_progress` precedent: outside the roadmap jsonb, owner RLS + table-level grants inherited, no new policies). **Storage only**: no LLM anywhere in the module, a write patches exactly the one column (roadmap/task_progress/gates/unlocks/reconnection unreachable by construction), the gate does NOT read these artifacts (evidence-aware gate prompts remain a future spec-guardian-gated change), and the Project Defense Report stays client-assembled in M13C — no report endpoint. `PUT` is an idempotent full-section replace stamped with a server-side `saved_at`; payloads are validated fail-closed (`schemas/workflow.py`: extra fields forbidden, string/list caps, http(s)-URL and 7–40-hex commit-hash checks on evidence kinds, fixed 8-item verification-check enum, 30 KB total-section cap, and secret-looking content rejected — `sb_secret_`/`sk-or-`/`AIza`/PEM markers); unknown stored keys are dropped on read. M13B also added `"PUT"` to the CORS allow_methods (first PUT route) and made `phase_service.require_phase` public. **Live-verified in M13B** (11/11 smoke checks: live column default on a pre-existing row, four-section round-trip through the real repository, full-section replace + phase isolation, rejected payloads stored nothing, live row-diff touched only `workflow_artifacts`, owner RLS read of the new column with a real JWT, zero cross-user visibility, leak-free client payload). The defense context pack (`services/defense_context_service.py` + `schemas/defense_context.py`, **M14A 2026-07-12**) is the deterministic, ownership-safe, provenance-aware evidence bundle the future artifact-aware gate (M14B) will ground questions in — **the gate does NOT consume it yet** (standalone internal service, no API route, no LLM import, read-only ProjectRepository seam; M14B wiring stays spec-guardian-gated). It normalizes project/phase/build-task-progress/intake plus the four workflow sections into purpose-built shapes, labels every source with a `SourceType` (student claims/evidence/verification are self-reported, never verified facts), treats missing artifacts as first-class `missing_sources` (never a failure), recursively redacts value-shaped secrets (`[REDACTED_SECRET]`; intake has no write-time guard — env-var *names* survive) before deterministic per-source + total (18k) character truncation with visible metadata, minimizes data (no user id/email/tokens/profile fields — tested), and renders via `render_defense_context` (fixed untrusted-data header + sorted-key JSON). The M14B seam is exactly `build_defense_context(project_repo, user_id, phase_number)` → `render_defense_context(pack)`; new public seams `phase_service.phase_view` and `workflow_service.stored_sections` support it. No migration; 23 new tests (360 total); live smoke 17/17 against real Supabase with StubProvider only (see `.claude/memory/defense-context-conventions.md`). **M14B (2026-07-12) — grounded artifact-aware defense questions:** the gate's three TURN questions now consume the pack — each turn call builds it and appends `grounding_service.context_block` (untrusted-data rules + per-turn grounding hints; the six prompt `.md` files are byte-identical, so the prebuild validator needed no change) to the composed prompt before the live-tuned tail; the cleaned question is then held to **deterministic grounding validation** (`services/grounding_service.py`): every code-shaped identifier must be supported by the pack/anchor/prior answers, proof-claims about self-reported records, accusatory framing, and calling a skipped/failed/n-a check "passed" are rejected; one corrective regeneration, then the existing retryable 502 (nothing stored; `_MAX_GROUNDING_ATTEMPTS=2` bounds spend). The structured contract is **server-derived** ({source_ids, grounding_terms} from the manifest's present sources — providers keep bare-text output; metadata stored inside the turns JSONB, whitelisted out of every client view; no migration). **The evaluator remains artifact-blind and byte-identical** (its prompt carries no context block — artifacts guide questions, never PASS/FAIL); `_HARD_LEAK` gained `system prompt`/`context pack` as the injection backstop; missing artifacts arrive as `missing_sources` the prompt must not invent (sparse context → anchor/phase fallback questions). 379 backend tests (19 new); live Gemini smoke 9/9 (grounded leak-free turns 1–3 on the pilot's verbatim `likes_score` anchor, planted artifact injection ignored, metadata internal-only); adversarial matrix in `docs/testing/m14b_grounded_defense_adversarial.md`; see `.claude/memory/grounded-defense-conventions.md`. **M14C (2026-07-12) — artifact-aware Project Defense UI + context readiness:** the M14B grounding is now visible without being exposed. Backend: `GET /gate/context-summary` (gate router) → `defense_context_service.build_context_summary` → `summarize_defense_context` — the ONLY pack-derived API shape: source ids + human display labels (`SUMMARY_LABELS`, deliberately separate from the honesty-bearing manifest labels the LLM sees) + source types + present/missing state + per-source truncation flags + `phase_number` (server-derived from `current_phase`; no client phase input) + `artifact_aware`; **never** artifact text, intake answers, rendered context, grounding terms, or identity data (leak-asserted against every content fixture in `tests/test_context_summary.py` — 16 new tests, 395 total). Pure read, no LLM, no DB write, no migration; ownership by construction (no id parameter — another user gets their own workspace state, usually 409); workspace not ready → 409, corrupt phase → 404. Frontend: the gate page fetches the summary **non-blocking on mount** (a slow or failed summary never gates the page or the Begin button — errors become a muted fallback line); `lib/defenseContext.ts` pure unit-tested helpers (Build Loop–ordered source chips, ONE grouped “Project context” pill for system sources, deterministic per-source preparation tips + sparse fallback “Your anchor and phase context are enough to begin.”, optional-never-error missing note with links to the workflow pages); ReadyView gains compact `pill ok` chips + two collapsed disclosures (“How are my questions personalized?” — recorded notes are self-reported, never proof; “Prepare in 30 seconds”); active questions carry a static “Grounded in your project” eyebrow (per-question source attribution is deliberately never inferred client-side); truncation is one muted line; the stale “gate doesn’t read your saved artifacts” rail copy was corrected (questions draw on recorded work; pass/fail stays about the student’s own explanation — the evaluator is artifact-blind and untouched). Gate API responses, evaluator, scoring, and cooldown byte-identical. 47 frontend tests (11 new); see `.claude/memory/artifact-aware-defense-ui-conventions.md`. **M15A (2026-07-13) — implementation import foundation ("Bring Back What AI Changed"):** a fifth workflow section, `implementation_import`, lets a student bring implementation material back into Codize after using an external AI tool — pasted AI response, git diff, code snippet, changed-file list, and/or their own summary. It is ONE `SECTION_MODELS` registration in the existing M13B architecture (same JSONB column, same generic GET/PUT routes, ownership, full-section-replace semantics, and server-stamped `saved_at`) — no migration, no new table, no new routes. Schema (`ImplementationImportArtifact`, `schemas/workflow.py`): required `source_kind` enum (ai_response/git_diff/changed_files/code_snippet/manual_summary/other); optional `content` ≤ 40k chars, `changed_files` ≤ 100×300 (deduped, empties dropped), `student_summary` ≤ 4k, `tool_name` ≤ 100; at least one of content/changed_files/student_summary required after normalization. Normalization is edges-only — internal indentation, line breaks, diff formatting, and Markdown are stored verbatim, and nothing is silently truncated (oversize → 422 naming the field, never echoing content). The serialized-body belt is per-section (`MAX_IMPORT_SECTION_CHARS` 100 KB for this section; others keep 30 KB — field caps stay authoritative). The shared secret-marker guard applies; a rejected save persists nothing and never echoes the value. Imports are UNTRUSTED student-provided material: no LLM anywhere (storage only), injection-shaped text stores inertly, and raw imports are deliberately NOT in the M14 defense context (its `_SOURCE_DEFS` manifest is fixed at 8 sources — tested; a future M15C/M16 may add a normalized Change Map via spec-guardian, never raw imports). M15C read seam: `workflow_service.get_implementation_import(project, phase_number)` → `StoredImplementationImport | None` (validated + saved_at; corrupt stored data → None). 437 backend tests (41 new + 1 extended); focused live smoke 22/22 against real Supabase with LLM_PROVIDER=stub (zero model calls, temp data cleaned to zero); frontend untouched. See `.claude/memory/implementation-import-conventions.md`. Routes: `GET /health` (public); auth-required: read-only `GET /archetypes` + `GET /archetypes/{id}` (`routers/archetypes.py`), the intake flow `GET /intake/questions`, `GET /intake/status`, `POST /intake/answers`, `POST /intake/complete` (`routers/intake.py`; out-of-order/premature/duplicate actions → 409, bad input → 422), the roadmap flow `POST /roadmap/generate` + `GET /roadmap` (`routers/roadmap.py`; not-ready/duplicate → 409, no roadmap yet → 404; since M13C.1B LLM failure or structure drift falls back to a valid template-backed roadmap and returns 200 active — invalid output is never stored, and only an invalid fallback, which is impossible by construction, would 502), the phase workspace `GET /phases`, `GET /phases/current`, `GET /phases/{n}`, `PATCH /phases/{n}/tasks/{task_id}` (`routers/phases.py`; workspace not ready → 409, unknown phase/task → 404), the gate flow `POST /gate/start`, `POST /gate/{id}/turn1|turn2|turn3|evaluate`, `GET /gate/current`, and the metadata-only `GET /gate/context-summary` (M14C — present/missing sources + labels + truncation flags, never content; workspace not ready → 409) (`routers/gate.py`; not ready/in progress/already passed/out of order/cooldown → 409 with Retry-After on cooldown, unknown session → 404, invalid anchor → 422, LLM failure/malformed verdict → 502 with nothing stored; a PASS's evaluate response includes `new_unlocks`), `GET /unlocks` (`routers/unlocks.py`; earned unlocks for the current project, empty list — not an error — before any project/roadmap; no separate "available" route because each phase view already exposes its `functional_unlock` description), the reconnection flow `GET /reconnection` + `POST /reconnection/acknowledge` (`routers/reconnection.py`; every outcome is a controlled 200 state — no-project/no-roadmap is `workspace_not_ready`, not an error), `GET /evaluation` (`routers/evaluation.py`; every outcome is a controlled 200 readiness state — pure read, no scores/thresholds/prompts in any response), and the workflow artifact store `GET /workflow/{phase}` + `PUT /workflow/{phase}/{section}` (`routers/workflow.py`; five sections since M15A — the four M13B sections plus `implementation_import`; workspace not ready → 409, unknown phase/section → 404, invalid or oversized payload → 422; since M15C.1 the GET also returns the top-level `change_map` view with its server-computed `stale` flag, and the Change Map lifecycle lives at `POST /workflow/{phase}/change-map/generate` — no import → 409, existing map without replace_existing → 409, LLM failure or invalid output → 502 with nothing stored — plus `PUT /workflow/{phase}/change-map` and `POST /workflow/{phase}/change-map/confirm` — no map → 404, invalid update → 422, stale/pending/duplicate-confirm → 409). The three archetype JSON templates live in `backend/app/templates/` and the seven system prompts in `backend/app/prompts/` (the spec's six plus the M15C.1 change-map extraction prompt) (see its README for call parameters). The database schema (4 tables: `profiles`, `projects`, `gate_sessions`, `unlocks`) is live in Supabase project `tadkbymxkdncqahzshml` with RLS verified — see `docs/db/schema.md` and `supabase/migrations/`. Supabase Auth is verified end-to-end with real JWTs (signup trigger, password login, RLS through PostgREST; `verify_auth.py` 11/11 re-run in M9 with the newer `sb_publishable_...`/`sb_secret_...` key formats, which every script and the backend treat as opaque strings) — see `docs/auth.md` for the audit and the backend auth enforcement design; env contract in `.env.example`. Live JWKS verification of a real Supabase JWT through `core/security.py` passed in M9. Still live-unverified: only the OpenRouter fallback path (Gemini has never failed during a probe).

**M16B.2 (2026-07-14) — Verification Suggestions and Student Results UI (current completed milestone; frontend-only, no migration):** the existing `/app/phase/verify` route now branches between linked M16B.1 Verification and the preserved manual checklist. Current complete linked Review exposes an explicit Start Verification action; missing/incomplete/stale Review gets a calm prerequisite and no automatic initialization. Linked checks group in the backend's six-category order and keep Review snapshot/rationale, immutable server suggestion, editable student check, exact result radios, and optional notes visibly separate. Progress means outcomes recorded—not checks passed—and the summary preserves pass/fail/skipped/not-applicable/unperformed. Canonical saves send only changed `verification_target_id` + `student_check`/`result`/`result_notes`; source/binding/stale fields never echo. Linked drafts reuse authenticated user + active-project + phase + safe binding/target-fingerprint scope, reject stale/rebuilt data, and keep manual drafts separate. Stale work stays readable/disabled and replacement requires an explicit warning; zero targets remain neutral. Recorded completion may include failure/skip/N/A and links to Evidence only, creating/prefilling nothing. Build Loop and phase next actions distinguish ready/in-progress/results-recorded/stale/zero while workflow capture stays N/5. Backend, schema, migration, prompts, providers, Defense, evaluator, Report, and Evidence data flow are unchanged. See `.claude/memory/linked-verification-ui-conventions.md`.

## Commands

- `python scripts/validate_prebuild_artifacts.py` — validates templates + prompts against the spec's invariants (run after any edit to either).
- `backend\.venv\Scripts\python scripts/live_adversarial_tests.py` — re-runs the live adversarial prompt suite (gate turns at 0.3, evaluator at 0) against the configured provider; results log in `docs/prebuild/adversarial_tests.md`. Run from the repo root (needs the gitignored `.env`) after any gate-prompt edit.
- `scripts/verify_rls.sql` — RLS/ownership audit queries; run via Supabase MCP `execute_sql` after any schema change (sections 6–9 expect permission errors; run per-section).
- `scripts/verify_auth.sql` + `python scripts/verify_auth.py` — end-to-end auth/RLS check over the real Auth+PostgREST APIs with real JWTs: run the SQL SETUP via MCP, run the script with `SUPABASE_URL`/`SUPABASE_ANON_KEY` set, then run the SQL CLEANUP. Run after any auth or RLS change.
- Backend tests: from `backend/`, `.venv\Scripts\python -m pytest` (venv setup: `python -m venv .venv` then `.venv\Scripts\pip install -r requirements.txt`). Run after any backend change.
- Backend dev server: from `backend/`, `.venv\Scripts\uvicorn app.main:app --reload`.
- Frontend (M13C, `frontend/` — Next.js App Router): from `frontend/`, `npm install`, then `npm run dev` / `npm run lint` / `npm run typecheck` / `npm run build` / `npm test` (vitest). See `frontend/README.md`. Env: copy `.env.example` → `.env.local` (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `NEXT_PUBLIC_API_BASE_URL` — publishable values only; never a service-role or provider key). Supabase is used for **auth only**; all product data flows Frontend → FastAPI (`lib/api.ts`, Bearer JWT). Run the frontend checks after any frontend change. **M13C.2 (2026-07-04)** completed the user-facing Build Loop: the live **Project Defense** gate flow (`app/app/gate/page.tsx` — anchor → 3 turns → evaluate over the M9 routes, resume-safe, not evidence-aware) and the full client-assembled **Project Defense Report** (`app/app/report/page.tsx` + pure, unit-tested `lib/report.ts`; Markdown copy/download; no report endpoint). No backend change. Client never receives gate scores / evaluator reasoning / thresholds / prompts; `defenseStatus` is current-phase-scoped, verification is labelled self-reported, missing evidence is shown as missing. Live browser smoke passed end-to-end (intake → roadmap → artifact → full gate PASS → report → export → logout). **M13D.2 (2026-07-05)** redesigned the public landing page (signature patch-loop terminal hero, git-log 80% Trap transcript, CI-style Build Loop pipeline, closing CTA section; fonts now actually loaded via `next/font` — DM Sans/IBM Plex Mono/Space Grotesk; zero new dependencies, reduced-motion safe, app interior untouched). **M13D.3 (2026-07-05)** turned the landing into six full-screen scenes: scroll-driven sticky patch-loop transcript with a draining control meter (`PatchLoopScene`), expanding-card Build Loop instrument panel (`BuildLoopPanel`), CSS-simulated glass Defense/Report panels, Cormorant Garamond editorial headlines (`--editorial` via next/font) — still zero runtime dependencies, no WebGL, reduced-motion/no-JS safe. **M13D.4 (2026-07-05)** fix pass from the user's visual review: Cormorant Garamond removed (headlines are Space Grotesk with violet — not italic — emphasis), every scene centered, the Build Loop made scroll-driven (sticky 320vh track selects the active card; click/focus stick until the scroll bucket changes; ≤900px/reduced-motion get a static/tap fallback), and the hero terminal wrapped in a premium pointer-tilt glass card (`TiltCard` — tilt/glow/traveling edge beams/sheen via CSS + style vars, fine-pointer only; the user's sign-in card reference was inspiration only, `/login` untouched, still zero new dependencies). **M13D.5 (2026-07-05)** precision fix pass from the user's second visual review: landing headlines unified with the body face (`.landing { --display: var(--sans) }` — DM Sans 700, larger hero at 64px; Space Grotesk stays app-interior-only), the landing made full-bleed (viewport-spanning header/footer bars, shared `clamp(20px, 4vw, 52px)` scene gutter, Build Loop row widened to 1280px), terminal text left-aligned inside the still-centered tilt card, Build Loop de-janked (fixed-height 276px card row so opening a card never resizes the row, 460vh track ≈ 50vh per stage, scroll hysteresis with a 0.22-stage dead zone), the confusing two-box Defense scene replaced by `GateScene` (scroll-revealed simulated chat: turn_01 → your_answer → follow_up → explain_implementation → `DEFENSE STATUS: READY` → recorded_in_report, labelled "simulated preview", fixed-height panel, no scores/evaluator reasoning), a compact evidence card after the trap scene (80% Trap framed as a named pattern — not a statistic — with two arXiv citations: Perry et al. 2211.03622, Fu et al. 2310.02059), and `/login` rebuilt as a centered non-scrollable glass screen (`.auth-screen`/`.auth-card` + `scrollTo(0,0)` on mount; Supabase auth logic byte-identical, no fake auth UI). Still zero new dependencies. **M13E.1 (2026-07-05) — core app usability + beginner guidance pass:** the protected app now uses the full screen (`.main` uncapped + centered `.main-inner` ≤1460px; `.workspace` two-column grid with a sticky `.ws-rail` guidance rail on cockpit/intake/prompt; sidebar becomes a wrapping top bar ≤840px — the app previously had NO mobile shell rules); first-use **"How Codize works" tutorial** (`components/Tutorial.tsx`, 9 steps, localStorage `codize:tutorial-seen`, auto-opens once after login — reconnection modal always wins — reopenable from the sidebar Help section, closes via button/Escape/backdrop unlike the spec-locked reconnection modal); **intake clarity** (per-question helper text + example placeholders + tap-to-fill chips — Q3 explains language/framework/database/"not sure yet", Q5 clarifies "first working version you can demo"; **answers editable until an explicit "Finish intake" review step** — the old auto-complete after Q5 was removed); the matching **small backend change** (`intake_service.submit_answer` accepts an already-answered question before completion — first-answer order, five-question contract, and post-completion lock unchanged; covered by new service+route tests, 314 backend tests pass); **Prompt Builder beginner layer** (`lib/phaseGuide.ts` — deterministic keyword-mapped plain-language explanation + starter asks for every template phase title, unit-tested; "What this phase means" panel, starter chips from the guide AND the phase's own AI-appropriate tasks, "use my intake answer/stack" chips, per-field hints); static **confusion help** (`details.help` glossary disclosures, `GuideCard` rail cards — no LLM chat); and the **multi-project audit**: deferred, `docs/plans/deferred/multi_project_dashboard_plan.md` documents that `get_project` = newest-row-wins so a second project would silently orphan the first — the cockpit shows a disabled "+ New project" affordance only. Zero new dependencies. Live smoke passed end-to-end (login → tutorial → intake with chips + live edit round-trip → roadmap → prompt builder chips → save/load → dashboard → mobile 390 → zero console errors). **M13F.1 (2026-07-06) — deployment readiness for the friend pilot (docs + tiny config, no product logic):** canonical hosted-deploy docs in `docs/deployment/` (`friend_pilot_deployment.md` — Vercel frontend + **Railway** backend recommended, Render free tier as the zero-cost fallback, Vercel Python functions explicitly rejected for this repo; `env_var_matrix.md`; `hosted_smoke_checklist.md`; `hosted_deployment_plan.md` marked superseded); committed `backend/Procfile` (uvicorn `$PORT` start) + `backend/.python-version` (3.12 host pin — local dev runs 3.14, code targets 3.11+); `CORS_ORIGINS` default now `http://localhost:3000,http://localhost:3001` (still never `*`; hosted deploys set the exact Vercel origin via env); signup now passes `emailRedirectTo: <origin>/login` so confirmation emails return to the origin the user signed up from (requires those URLs in Supabase Auth → URL Configuration — a dashboard change, documented, not scripted). Safety-limit audit: existing input caps (intake 4000 / gate anchor 2000 · answers 8000 / workflow 30 KB), one-attempt-per-provider LLM calls with no retry loops, gate cooldown, and auth on every LLM route already bound pilot spend; per-user quotas/IP rate limiting stay deferred per spec. **M13E.2 (2026-07-06) — pilot bugfix pass 1 (first real friend test):** gate anchor validation is now **two-tier** (`gate_service`): an anchor containing a code-shaped identifier (backticks, snake_case, dotted/path, "the variable is called likes_score") makes the deterministic server check authoritative — the Turn 1 composition tail tells the model NOT to re-reject it, and a disobedient `ANCHOR_REJECTED:` becomes a retryable 502, never a 422 (element-type-only "weak" anchors keep the M9 model re-validation; the 422 help copy now shows concrete examples like `likes_score` / `app/models.py`); the question cleanliness guard was **hardened** against the pilot's reasoning leak ("Therefore, it is a valid anchor. Now I need to formulate…") — markdown/label-line stripping, extended hand-off + meta-sentence patterns, re-unwrap of announced quoted questions, plus a `_HARD_LEAK` backstop rejecting any cleaned question still carrying rubric/evaluator/internal vocabulary (retryable; prompts and evaluator untouched); the frontend gained **localStorage draft persistence** (`lib/drafts.ts` + unit tests — all four workflow pages, gate answers, intake; keys scoped `codize:draft:<user>:<surface>`, secret-marker guard mirroring `schemas/workflow.py`, drafts overlay after backend prefill and clear on successful save with a one-shot echo skip), **wide-screen layout** (every core page now uses the `.workspace` rail grid; `.main-inner` 1620px, 400px rail ≥1800px; gate explainer and phase roadmap/progress cards moved into rails), **Verification Lab result-specific note prompts** (pass→how checked, fail→what broke, skipped/n/a→optional reason + "no evidence needed"; `lib/report.ts` labels skipped/n/a honestly and never prints the raw enum), and **progress clarity** ("Build tasks: X/Y" vs "Workflow: N/4 captured" on the phase page and cockpit, with explicit "saving artifacts never ticks a build task" copy). `GEMINI_MODEL` swap guidance documented (backend host env var, e.g. Railway — no code deploy; committed default unchanged; gate cleanliness never relies on a model upgrade). 337 backend + 36 frontend tests pass; live smoke against real Gemini: the tester's verbatim `likes_score` anchor accepted, Turn 1+2 questions leak-free, drafts survived tab switches and gate resume, skipped/n/a saved without evidence, zero new console errors. **M13E.3 (2026-07-07) — cognitive-load reduction + guided app UX pass (frontend-only; no backend/schema/prompt/evaluator change):** the pilot's deeper feedback ("overwhelming, text-heavy"; Prompt Builder rated 5/5, Evidence felt like homework, gate felt punishing) is addressed with a **progressive-disclosure rule** across the protected app — `GuideCard` is now a collapsed `<details>` (title visible, body on demand), max 1–2 explanatory sentences visible per screen top, chips/placeholders over hint paragraphs, no repeated workflow explanations — and **one accented primary action per screen**: cockpit restructured to "Do this next" hero (state-aware button) + one compact project/progress card + loop strip + unlocks-only-when-earned; phase page gained a "Next step" card (first uncaptured Build Loop section, else the gate) and one-line task-card intros; **Prompt Builder became the hero surface** (Step 1 = the ask + merged starter chips, Steps 2–3 = short-labeled optional context/guardrails, quick-add guardrail chips incl. "Plan first"/"Do not touch auth"/"Give manual verification steps" via append-only `addConstraint`); **Evidence reframed** as "proof you checked something — one small piece is enough" (5 primary kind chips, technical kinds behind "More types", explicit "skip for now →", honesty fine print kept in a collapsed rail card); Verification reads as a quick honesty check with "come back later" language; **gate copy is coaching** ("not a test of intelligence", "keep your code open", example anchors + "what makes a good answer" as collapsed details, softened fail/cooldown copy — evaluator strictness untouched); new `components/LoopOverview.tsx` (the 8-line "what you'll actually do" answer, collapsed, on the cockpit hero). Pilot lesson recorded in `.claude/memory/pilot-ux-lessons.md`. Frontend checks green (typecheck/lint/36 vitest/static build); live smoke: dashboard→next action obvious, all pages calm at 1920px, drafts still persist, skipped/n-a still saves, report loads, zero overflow at 390px, zero new console errors. **M13E.4 (2026-07-12) — skill-driven product UI polish pass (CSS system + presentational class swaps only; no behavior, backend, or copy-semantics change):** the protected app's visual system matured in `globals.css` — new **`.card.primary`** signature (2px accent left rail + sole hover lift) replaces inline violet `borderColor` on each screen's one primary card, echoed by the sidebar active link's inset rail; **resting glow removed** from `.btn.primary` (hover-only depth, `:active` press scale on all buttons/chips); status pills/notices softened with `color-mix` borders and **cooldown demoted from red to amber** everywhere (gate/cockpit pills, report defense status); `details.help` and the Build Loop strip are **borderless** (tint-on-surface — kills the card-in-card look); generic card hover reaction removed; type scale raised (page-title 26px, page-sub 62ch) and **textareas switched from mono to sans** (mono now only for code-like output); three transform/opacity motions added within the existing prefers-reduced-motion kill switch (`reveal` on disclosure bodies, `rise` on the generated-prompt payoff, focus rings via 3px color-mix box-shadow); report "Copy as Markdown" promoted to the primary button. Designed against the installed `ui-ux` + `frontend-design` skills (the task's named Impeccable/Taste/Emil skills are not installed). Checks green (typecheck/lint/36 vitest/static build); live smoke re-verified drafts, skipped/n-a saves, collapsed guidance, prompt payoff animation, zero overflow at 1920/390, zero in-session console errors. See `.claude/memory/frontend-conventions.md`. **M15B (2026-07-13) — "Bring Back What AI Changed" UI (frontend-only; zero backend change):** the M15A implementation import got its student-facing page, `app/app/phase/import` — the loop's missing middle between using the prompt externally and reviewing the result. Prevention-first framing ("keep track of what changed before moving on"; "You do not need every item"), recovery as one collapsed rail card. One primary card: a semantic fieldset radio-chip picker mapping six plain-language kinds to the M15A enum (never shown raw; selecting a kind never clears entered content), the kind-relevant main field emphasized (content textarea for ai_response/git_diff/code_snippet/other — mono `textarea.code`, verbatim material, honest `N / 40,000` counter, NO maxLength so large pastes are never silently clipped; changed-files or summary emphasized in their modes), everything else behind a collapsed "Add more detail (optional)" (including a generic content field in non-content modes so no state is ever invisible), and a git-diff explanation disclosure only in git_diff mode. Save is the existing generic workflow client (`PUT /workflow/{phase}/implementation_import`, full-section replace explained in plain language when editing); `saveBlocker` disables Save until content/files/summary carries something and names any over-limit field without echoing content — the backend stays authoritative. Drafts ride the M13E.2 layer (`implementation_import:<phase>`), with a visible warning when the secret-marker guard refuses to keep a credential-like draft locally; the backend's 422 shows as-is and never echoes the value. Integration: `WorkflowSectionName`/`WorkflowSections`/api.ts `SectionPayloadMap` gained the fifth section; the Build Loop strip is 8 steps ("Bring Back" after Generate), sidebar + phase "Next step" ordering carry it, cockpit/phase progress reads "Workflow: N/5 captured" (the backend returns five keys since M15A — the old /4 would have displayed 5/4), and the Prompt Builder hands off ("Then bring back what changed →") after its artifact saves. Pure helpers in `lib/implementationImport.ts` (source options, form ↔ payload shaping, blockers, page copy constants) are unit-tested; SaveBar gained an optional `disabled` prop; new CSS primitives `label.chip` (keyboard-operable radio chips with a :has(input:focus-visible) ring) and `textarea.code`. No LLM call, no Change Map (M15C), report untouched, raw imports still absent from the defense context (live-checked: 8 sources before/after imports). 83 frontend tests (36 new); backend suite still 437; live browser smoke passed end-to-end (activation → prompt save + handoff → git-diff import with byte-identical round-trip → replacement via summary-only and changed-files-only saves with backend dedupe visible → draft survives refresh, phase-scoped key → fake secret: draft refused with warning, 422 shown without echo, form preserved → Build Loop status updates → 390px no overflow → arrow-key source selection with visible focus ring → zero unexpected console errors). See `.claude/memory/implementation-import-ui-conventions.md`. **M15C.1 (2026-07-13) — secure, provenance-aware Change Map extraction foundation (backend only; no frontend change, no migration):** the saved implementation import can now be converted into an AI-generated, EDITABLE DRAFT Change Map — "this appears to be what changed; review and correct it", never a correctness claim. Domain model (`schemas/change_map.py`, strict/extra-forbid): 8 categories (changed_file/behavior_change/implementation_decision/out_of_scope_change/security_sensitive_area/unresolved_risk/unverified_behavior/question_to_understand), origin (`ai_inferred`/`student_added`) kept separate from the student decision (pending_review/confirmed/edited/rejected/uncertain/needs_inspection) and from AI uncertainty (supported/ambiguous/needs_inspection — deliberately no numeric confidence); `draft_text` (server-owned after generation) separate from `student_text` (stored only for edited/student_added); bounded source references (≤5×300-char excerpts). Persistence is a SIBLING key in the same JSONB (`workflow_artifacts[phase]["change_map"]`) — deliberately NOT a workflow section: the generic section PUT 404s on it, `stored_sections` filters it out (so the M14 defense context stays at its fixed 8 sources by construction), and `GET /workflow/{phase}` returns it top-level with a server-computed `stale` flag (never inside `sections` — protecting the frontend's N/5 counts). Generation (`POST /workflow/{phase}/change-map/generate`, temp 0, `prompts/change_map_extraction.md`): typed M15A read seam → full M14A `redact_secrets` per field BEFORE deterministic truncation (summary 4k / files 12k whole entries / content 20k head+tail, visible markers, cuts never split the redaction marker; the stored import is never mutated; honest `source_redacted`/`source_truncated` metadata) → explicit untrusted-data delimiters → fail-closed parse (`GeneratedChangeMap`, ≥1/≤40 items) → deterministic provenance validation (references must target present fields, kind must match, excerpts must be verbatim substrings of the sanitized view — edges canonicalized, whitespace-only rejected — file paths must exist in the material) + identifier grounding (reuses `grounding_service.extract_grounding_terms`; M14B untouched) → one corrective regeneration (categories only, never raw output/material) → retryable 502 with nothing stored. The server assigns item ids (deterministic hash), origin, pending_review, timestamps, status, and `source_import_saved_at` (replacing the import makes the map stale — derived on read, stale maps cannot confirm, explicit `replace_existing` required to overwrite any existing map, confirmed included). Lifecycle (no LLM): `PUT /workflow/{phase}/change-map` edits only student-owned fields (updates by item id + full-replace `student_added_items`; any update returns the map to draft); `POST /workflow/{phase}/change-map/confirm` blocks on pending/stale, allows rejected/uncertain/needs_inspection honestly, stamps `confirmed_at` server-side. `StubProvider` gained a change-map branch (same well-behaved-model pattern) so stub smokes exercise the full pipeline. M16 seams (not consumed): `workflow_service.get_change_map` + `change_map_service.confirmed_items`/`unresolved_items` (confirmed→draft_text, edited/student_added→student_text, rejected→excluded). 546 backend tests (109 new); deterministic live smoke 39/39 against real Supabase with LLM_PROVIDER=stub (temp data cleaned); live Gemini runs verified the fail-closed path (invalid provenance → 502, nothing stored, category-only logs) and drove two hardenings (single-line verbatim-excerpt prompt rule; excerpt edge canonicalization + whitespace-only rejection) — a fully valid live draft was not achieved on gemini-2.5-flash-lite in the run window (env-swappable `GEMINI_MODEL`; completion deliberately does not depend on live quota). Adversarial matrix in `docs/testing/m15c_change_map_adversarial.md`; see `.claude/memory/change-map-conventions.md`.

**M15C.2 (2026-07-13) — Change Map Review, Editing, Uncertainty, and Confirmation UI (latest completed milestone; frontend-only, no migration):** `/app/phase/change-map` now completes the student-facing lifecycle over the exact M15C.1 contract. Generation is explicit only (normal POST has no body; deliberate replacement alone sends `replace_existing=true`); safe 502 UX preserves the import/current map and offers retry plus import review. Strict frontend types mirror the top-level map, eight categories, three uncertainty states, six student decisions, bounded references, student-only update body, and confirmation response. `lib/changeMap.ts` holds unit-tested labels/grouping/progress/effective display/validation/payload/dirty/readiness/stale/page-state/phase-next-step logic. The page visibly separates Codize draft / student correction / student-added / rejected / uncertain / needs-inspection; source references are collapsed, bounded, plain text and explicitly not proof; supported is never called verified and numeric confidence never appears. Review saves explicitly; local drafts reuse `useDraft` with user + phase + generated-map-version scope, secret guard, and stale/regeneration invalidation. Confirmation means reviewed, not correct, and unresolved items remain valid. Confirmed-map edits are opt-in and return to draft only after Save. Stale maps remain visible but cannot confirm; regeneration warns that review decisions are replaced. The shell, Build Loop, cockpit, phase next action, and import post-save handoff include Change Map after Bring Back, while artifact progress remains N/5. Review Board, Evidence, Verification, Defense Context, Project Defense, evaluator, and Defense Report data flow remain untouched until M16. See `.claude/memory/change-map-ui-conventions.md`.

**M16A.1 (2026-07-13) — Confirmed Change Map → Review Integration Foundation (latest completed milestone; backend/data-contract only, no migration):** the existing phase-scoped `review_board` JSONB section can now be initialized deterministically from the owned phase's current confirmed, non-stale Change Map via `POST /workflow/{phase}/review/from-change-map` (no body or `{"replace_existing": true}` only). `review_service.create_from_change_map` uses only the typed M15C `confirmed_items` / `unresolved_items` seams and filters six implementation-relevant categories in fixed order; changed-file/question items remain context, rejected/pending items never become targets, and uncertain/needs-inspection items retain an explicit unresolved source state. Each target stores a deterministic server id, Change Map id/category/origin/student-decision snapshot, bounded effective-text snapshot, and student-owned `pending` Review decision. Source generation/confirmation timestamps bind the draft; `GET /workflow/{phase}` computes linked Review `stale` on read when the map is missing/draft/import-stale/regenerated/reconfirmed. The existing Review PUT remains compatible and now accepts additive `target_updates` (pending/keep/revise/remove/needs_verification/uncertain + student rationale/revision) while all provenance, snapshots, ids, timestamps, and stale state are copied server-side and cannot be forged. Existing Review work is never overwritten without explicit replacement; manual/legacy Review remains valid. Progress helpers are Review-only; `needs_verification_targets` is the exact typed M16B seam but creates nothing. Raw imports/references never enter linked Review, no provider is called, no frontend/Defense/Evidence/Verification/evaluator/report behavior changed. See `.claude/memory/change-map-review-integration-conventions.md`.

**M16A.1 verification:** 597 backend tests pass; the authenticated deterministic smoke passes all 40 canonical checks; no frontend file or database migration changed.

**M16A.2 (2026-07-13) — Confirmed Change Map → Review UI (frontend-only, no migration):** `/app/phase/review` now preserves two compatible modes: linked Review initialized explicitly from a confirmed/current Change Map, and the unchanged legacy/manual form for existing artifacts. No initialization runs on mount. Linked targets render in the backend's six-category order with bounded source snapshots as plain text and honest confirmed/corrected/student-added/unresolved labels; students choose pending/keep/revise/remove/needs_verification/uncertain through semantic radios. `lib/review.ts` owns strict mode detection, labels, grouping, Unicode-safe 2,000-character validation, progress/completion, canonical student-only `target_updates`, dirty comparison, safe source-binding fingerprints, scoped draft restore, stale/replacement readiness, and Build Loop status. Hidden rationale/revision text is excluded from payload and dirty state. Unsaved linked decisions reuse `useDraft` under user + active-project + phase + source-binding/ordered-target scope; stale/rebuilt Reviews reject old drafts. Stale Review remains readable but disabled and replacement requires an inline warning before `replace_existing=true`. Zero targets remain neutral; uncertainty and needs-testing count as reviewed. Completion links to `/app/phase/verify` only—no Verification suggestions/checks, Evidence, Defense, or Report integration. `WorkflowSteps` distinguishes ready/in-progress/complete/stale and phase next-action logic handles Start/Continue/Rebuild/Continue-to-Verification while manual Review keeps its established evidence-first continuation. Workflow capture remains exactly N/5. Backend, database, prompts, providers, and migrations are unchanged. See `.claude/memory/linked-review-ui-conventions.md`.

**M16B.1 (2026-07-13) — Review decisions → Verification suggestion foundation (backend/data-contract only, no frontend change, no migration):** the existing phase-scoped `verification` JSONB section can now be initialized explicitly from the owned phase's saved, complete, current linked Review via `POST /workflow/{phase}/verification/from-review` (no body or `{"replace_existing":true}` only). `verification_service.create_from_review` consumes only `review_service.needs_verification_targets(review)`, preserving its deterministic order and excluding pending/keep/revise/remove/uncertain. Six explicit category templates embed the bounded reviewed effective text and produce beginner-actionable proposed checks with zero Gemini/OpenRouter/stub calls, no prompt, no execution, no invented project details, and no completed result. Each target stores deterministic server `vt-...` identity, Review/Change Map ids, category, bounded source/rationale snapshots, suggestion, and nullable future student check/result/notes; null is unperformed and the existing pass/fail/skipped/not_applicable semantics are unchanged. The binding records source Change Map timestamps, Review saved_at, and an ordered identity/decision fingerprint; `GET /workflow/{phase}` exposes additive linked fields plus server-computed stale when Review changes/disappears/corrupts/rebuilds/stales/rebinds. Stale work remains readable. Existing manual and linked Verification never overwrite without explicit replacement. The generic Verification PUT remains compatible with the current frontend and now protects all server fields while accepting only student-owned `target_updates`. Zero needs-testing targets create an honest empty linked artifact, never completion. Typed pending/performed/failed/unresolved and future Evidence handoff helpers create no Evidence. Linked source/suggestion data remains absent from Defense Context/Project Defense/evaluator/report. 62 focused backend tests include a 43-check authenticated deterministic smoke. See `.claude/memory/review-verification-integration-conventions.md`.

**M16B.3B (2026-07-14) — Verification Results → Student Evidence UI (frontend-only, no migration):** `/app/phase/evidence` now preserves legacy/manual Evidence while consuming M16B.3A linked Evidence through a pure preview GET, explicit zero-default selection, explicit initialization POST, and changed-only student target updates through the generic Evidence PUT. The client trusts server eligibility and completion, never creates Evidence on mount, preselects targets, copies Verification results/notes into entries, or sends provenance. Each linked target separates read-only Verification context from the student's not-addressed/recorded/unavailable decision; recorded and unavailable have mutually exclusive validated fields. Drafts are user/project/phase/fingerprint scoped with no source text. Stale work is readable/disabled and replacement requires a fresh zero-selection preview plus explicit warning. Workflow status/next action now distinguish ready/in-progress/complete/stale without changing N/5. Defense, Report, gate, evaluator, backend, prompts, providers, and schema remain unchanged pending M16C. See `.claude/memory/linked-evidence-ui-conventions.md`.

**M16C.2 (2026-07-14) — Artifact-Aware Project Defense and Defense Report UI (frontend-only, no migration):** `/app/gate` now consumes only the M16C.1 metadata summary for Change Map/Review/Verification/Evidence readiness, exact source states, and truncation; the independent summary load can fail or retry without blocking the unchanged Defense lifecycle. New attempts explain the stable server-owned record, while answers remain empty, student-owned, draft-safe, and sent only through the existing anchor/turn/evaluate bodies. `/app/report?phase=N` now reads the authoritative `GET /report/{phase}` response instead of reconstructing a mutable client report. It distinguishes attempt snapshot from legacy current-workflow fallback, preserves the backend truth notice, provenance, uncertainty, failed/skipped/n-a/unrecorded Verification, linked/manual/stale/unavailable/not-addressed Evidence, the public transcript, and exact PASS/FAIL outcome without proof claims or hidden internals. HTML-like values are plain text and only validated HTTP(S) Evidence URLs become safe external links. Cockpit/phase handoffs now reach Defense/Report without changing N/5. Backend, gate behavior, evaluator, prompts, providers, models, schema, and migrations are unchanged. See `.claude/memory/artifact-aware-defense-report-ui-conventions.md`.

## Product and Context Sources

Before making a product, workflow, or major UX decision, read:

- `docs/context/context_authority.md`
- `docs/context/codize_product_operating_brief_v2.md`

Use `docs/context/codize_master_spec_v2.1.md` only for still-applicable architecture and safety invariants.

Current code, tests, migrations, Git history, and deployment evidence determine what is implemented.

Do not load archived plans, old product visions, model guides, or chat exports unless the task specifically requires historical context.

## Planned architecture

Built strictly back-to-front through external services — **never Frontend → External Service directly.**

- **Frontend (Next.js, deployed on Vercel) → Backend (FastAPI, deployed on Railway) → External Service (Supabase, LLM provider APIs).**
- **Supabase** for database + auth. **FastAPI** (async — required, every LLM call is I/O-bound) for the API. **Gemini (primary) / OpenRouter (fallback)** for the AI engines via the provider-agnostic `llm_service`; Anthropic is intentionally not supported.
- Backend structure: a **`services/` layer holds all AI calls, gate logic, and template loading — never in route handlers.** A `templates/` folder holds the three archetype JSON files, loaded at startup.
- MCP tooling is available for filesystem, git, github, supabase, and playwright — use the supabase MCP to *verify* schema/RLS rather than assuming.

## Build order & milestone workflow (important)

Do not build the whole product in one session. Work **exactly one milestone at a time.**

**Pre-build gate (spec "Pre-Build Artifacts Required"):** before *any* backend/schema code, both must exist —
1. The **three archetype JSON templates** (full phase structure for all three archetypes).
2. The **six system prompts** (per the spec's "System Prompt Architecture"), drafted in full and manually tested against adversarial inputs.

After the pre-build artifacts, the historical milestone sequence was: Repository foundation → Supabase schema + RLS → Auth → FastAPI core → Archetype template engine → Intake → Roadmap generation → Phase workspace → Interrogation Gate → Functional unlocks → Reconnection → Evaluation → AI Workflow Workspace MVP → Security audit → Deployment (Vercel + Railway). Inspect Git history and repository state for what is complete; do not use this sequence as a current implementation tracker.

For each milestone: **Implement → Test → Verify → Commit → Update memory → Stop.** Then output `MILESTONE COMPLETE` with files changed, tests run, verification results, and the git commit hash, and instruct the user to `/compact`, start a fresh session, and paste the continuation prompt. Do **not** auto-continue into the next milestone.

Maintain lessons in `.claude/memory/` (one specific lesson per file; update or remove stale ones); consult it before major architectural decisions.

## Domain invariants that must not drift

Fixed by the spec — do not add, remove, or reorder:

- **Intake:** exactly five mandatory, sequential, conversational questions. Question 1 is verbatim "What problem do you want to solve, and who does solving it help?" and cannot be skipped. Store all five answers (purpose, scope, stack, self-assessed understanding, timeline). Signup goes straight to question 1 — no form/dashboard/homepage.
- **Archetypes:** exactly three — (1) AI-Powered App, (2) REST API Backend, (3) Full-Stack Web App. No fourth, ever. Structure comes from hardcoded JSON templates; the LLM personalizes wording only and may never add, remove, or reorder phases, change AI-vs-human task labels, or alter gate targets/unlock conditions. Classification is a temperature-0 call. Tiebreaker: LLM API is a core feature → Archetype 1; else has frontend/database → Archetype 3; else → Archetype 2. Default stack for Archetype 1 is fixed: Python + FastAPI + Vanilla HTML/JS.
- **Interrogation Gate:** mandatory, cannot be skipped (student may choose Option A explanation / Option B bug-hunt format; A is the MVP default). Requires a self-reported anchor statement, then Turn 1 (implementation-specific question, temp 0.3), Turn 2 (probe the weakest of accuracy/specificity/completeness), Turn 3 (fresh hypothetical from anchor + prior answers + implementation, not answerable from generic knowledge). On failure: no immediate retry, **30-minute cooldown**.
- **Evaluation:** a **separate** LLM call at **temperature 0**, binary PASS/FAIL + one-sentence reason + a 0–10 quality score, against three conditions (Structural Identification, System Ripple Effect, Implementation Specificity — all three required). **Auto-fail any textbook/generic answer** that could apply to any codebase regardless of technical correctness.
- **Gate eval is a two-call pattern:** Call 1 (temp 0.3) generates the Turn 3 hypothetical; Call 2 (temp 0) evaluates the answer. (Phase explanations use temp ~0.7.)
- **Functional unlocks:** triggered by performance thresholds the student cannot observe (e.g. quality score ≥7 across two consecutive gates), never by phase completion. No badges/XP/streaks/leaderboards.
- **Reconnection:** in-app modal only (no email). Triggers on login when `last_login` delta > 72h; shows the student's verbatim intake purpose; dismissed only by clicking "Let's keep building" (no timer, no click-outside, no Escape).

## Security constraints (non-negotiable)

Encoded into every archetype template from Phase 1, not bolted on at the end:

- **Secrets stay server-side.** Never expose LLM provider keys (Gemini/OpenRouter), Supabase service-role keys, or DB secrets to the frontend — `.env` values get bundled into JS. Frontend calls the backend; the backend calls external APIs with the real key. (OWASP A02.)
- **RLS on every Supabase table before any other DB code.** Each table has a `user_id` column and an ownership policy (`USING (auth.uid() = user_id)`). "User is logged in" is not ownership. Verify with the supabase MCP. (OWASP A01.)
- **Auth enforced server-side on every protected endpoint** — UI hiding/redirects are not security. Return 401 unauthenticated, 403 for another user's resource. (OWASP A01.)
- **Input validation** (parameterized queries, output escaping) on any user input reaching the DB or the UI. (OWASP A03.)
- Every archetype ends with a mandatory, gate-checked **pre-deployment security checklist** phase.
- At each milestone, run a living audit of tables, policies, and ownership checks; record findings. Advanced hardening (rate limiting, DDoS, full OWASP audit, pentest, CSP) is explicitly out of scope for the MVP.

## Engineering style

Build the simplest robust thing that satisfies the current task — no speculative abstractions, premature optimization, hypothetical scaling layers, or future-proofing. Trust framework guarantees; validate only at system boundaries (user input, external APIs). Escalate reasoning effort only for gate logic/prompts/rubrics, security audits, and evaluation systems.

Before adding anything, check it against the spec: is it explicitly required, is it needed for the current milestone, can it be done more simply? If no, don't build it. The "temptation to add a fourth archetype will come — resist it."

Report only verified work: audit each completion claim against actual test/tool/MCP output. If something is unverified or failed, say so — never fabricate progress.

## Guided project shell (M16N)

Protected-app navigation has one source of truth:
`frontend/lib/guidedProjectNavigation.ts`, loaded by
`GuidedProjectNavigationProvider`. Project Home (`/app`) is always first;
Continue selects the earliest saved actionable/stale dependency; Journey shows
the implemented Prompt → Import → Change Map → Review → Verification →
Evidence → Project Defense → Defense Report order; Project Record holds saved
and historical deep links. Future Journey rows are non-interactive. Active
route (`aria-current`) is independent from lifecycle status.

Never progress this shell from local drafts, the currently open route, or
linked Evidence presence. Use saved artifacts, confirmed/stale Change Map,
saved Review/Verification decisions, server `evidence_record_complete`, and
evaluation/gate lifecycle. Manual and legacy workflow order remains supported;
stale records stay readable and are rebuilt only by the existing explicit page
actions. Desktop and the focus-managed mobile drawer consume the same model.
Do not create a parallel sidebar or change workflow N/5.

## Skill usage

Before implementation, read the relevant local skill files under `.claude/skills/`:

- `.claude/skills/spec-guardian/SKILL.md` — product invariants and spec alignment (read before any product-logic change).
- `.claude/skills/security-test/SKILL.md` — validation, pytest, secret scanning, RLS checks, Playwright checks.
- `.claude/skills/ui-ux/SKILL.md` — Codize visual style (dark, high-contrast, violet accent), layout, and interface rules.
- `.claude/skills/milestone-handoff/SKILL.md` — milestone completion, git commit, memory update, and `/compact` handoff protocol.

If a reusable correction or durable engineering lesson is learned, update the relevant skill file or create/update one concise, specific file in `.claude/memory/` (one lesson per file — no vague notes).
