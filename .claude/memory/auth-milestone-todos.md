# Auth carry-over items (updated after Milestone 4, 2026-07-02)

1. **Leaked-password protection: advisor-clean as of end of M3 (2026-07-02).** The WARN was present at session start and gone on two advisor re-runs at session end — it is a Dashboard-only toggle (Authentication → Sign In / Providers → Passwords), so it was evidently enabled in the dashboard mid-session. Re-check `get_advisors` each milestone; if the WARN returns, that toggle is the fix.
2. **Email confirmations are ON** — new signups get no session until the confirmation link is clicked. The frontend milestone (M13) must handle the "check your email" state, or confirmations must be deliberately disabled then. Also: the built-in email sender rate-limits after ~2 sends/hour and GoTrue MX-validates signup email domains (fake domains → `email_address_invalid`).
3. The signup→profile trigger (`handle_new_user`) exists and is verified end-to-end; never recreate it.
4. Backend JWT verification is **implemented** (M4): `backend/app/core/security.py` (JWKS/ES256, `aud == "authenticated"`, id = `sub`) + `backend/app/deps/auth.py` (`require_user` → 401). Tests sign with a local ES256 key and stub only the JWKS fetch, so **verifying a real Supabase JWT over the live JWKS URL is still unverified** — do it in the first backend session that has `SUPABASE_URL` set (login via `scripts/verify_auth.sql` users, hit a protected route). The service-role rule stands: backend bypasses RLS, so every user-scoped query must filter by `user_id == sub` itself.
5. Live adversarial prompt testing is now UNBLOCKED (`GEMINI_API_KEY`/`OPENROUTER_API_KEY` exist in the gitignored repo-root `.env` since M8; the M8 live roadmap smoke test proved the Gemini path) but has NOT run — execute it at the start of Milestone 9, before gate runtime code. Never ask the user to paste a key into chat.
6. `scripts/verify_auth.py` was NOT run in M4–M8 (`SUPABASE_URL`/`SUPABASE_ANON_KEY` are still empty in `.env`); M8's schema change (added `projects.task_progress` jsonb column) touched no policy/grant — RLS re-checked structurally via MCP in the M8 session (all 4 tables enabled, 9 policies unchanged, security advisors clean), so M3's 11/11 verification stands. (M5's `/archetypes`, M6's `/intake/*`, M7's `/roadmap*`, and M8's `/phases*` routes reuse the already-tested `require_user` dependency.) Re-run it whenever those env vars are filled or anything auth/RLS-adjacent changes.
7. M6's `SupabaseProjectRepository` (PostgREST with the service-role key, ownership filters in every query) has **never made a live request** (`SUPABASE_SERVICE_ROLE_KEY` still empty in `.env`) — verify intake reads/writes, M7's roadmap JSONB write + status flip, AND M8's `task_progress` writes end-to-end in the first session with those env vars set.
