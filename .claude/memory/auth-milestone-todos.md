# Auth carry-over items (updated after Milestone 9, 2026-07-02)

1. **Leaked-password protection: advisor-clean as of end of M3 (2026-07-02).** The WARN was present at session start and gone on two advisor re-runs at session end — it is a Dashboard-only toggle (Authentication → Sign In / Providers → Passwords), so it was evidently enabled in the dashboard mid-session. Re-check `get_advisors` each milestone; if the WARN returns, that toggle is the fix.
2. **Email confirmations are ON** — new signups get no session until the confirmation link is clicked. The frontend milestone (M13) must handle the "check your email" state, or confirmations must be deliberately disabled then. Also: the built-in email sender rate-limits after ~2 sends/hour and GoTrue MX-validates signup email domains (fake domains → `email_address_invalid`).
3. The signup→profile trigger (`handle_new_user`) exists and is verified end-to-end; never recreate it.
4. Backend JWT verification is **implemented (M4) and LIVE-VERIFIED (M9)**: `backend/app/core/security.py` (JWKS/ES256, `aud == "authenticated"`, id = `sub`) verified a real Supabase JWT (password login as a `verify_auth.sql` test user) over the live JWKS URL. The service-role rule stands: backend bypasses RLS, so every user-scoped query must filter by `user_id == sub` itself.
5. Live adversarial prompt testing **COMPLETED in M9** — see `docs/prebuild/adversarial_tests.md` and `.claude/memory/live-prompt-testing-pending.md`. Never ask the user to paste a key into chat.
6. `scripts/verify_auth.py` **re-run in M9: 11/11 PASS** over the live Auth + PostgREST APIs using the newer `sb_publishable_...` anon key (keys are passed opaquely in the `apikey` header — no JWT-shape assumption; works with legacy keys too). M9's schema change (`gate_sessions.passed_at` + column grant) verified via grant dump: authenticated selects every gate column except `score`; advisors clean. Re-run whenever anything auth/RLS-adjacent changes.
7. `SupabaseProjectRepository` and `SupabaseGateSessionRepository` **LIVE-VERIFIED in M9** with the `sb_secret_...` key: project create/update, roadmap JSONB write + status flip, gate session create/update, `current_phase` advancement, ownership filtering (user B fetching A's session → None). Test rows cleaned via `verify_auth.sql` CLEANUP (cascade left zero rows). Still live-unverified anywhere: the OpenRouter fallback path.
